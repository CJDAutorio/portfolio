import React from 'react';

import './styles.css';

const Landing: React.FC = () => {
    return (
        <div>
            <h1>This is the landing page.</h1>
        </div>
    );
};

export default Landing;